{
  "name": "raven-server",
  "version": "0.1.2",
  "description": "Project Raven",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "(cd api && npm install); (echo 'Server and API dependencies have been installed.')",
    "start": "PORT=80 NODE_ENV=production ENV=production forever server.js",
    "development": "(cd api && npm run start)"
  },
  "author": "Pixxl",
  "license": "MIT",
  "devDependencies": {
    "@angular/cli": "^1.6.8",
    "express": "^4.16.3"
  }
}
